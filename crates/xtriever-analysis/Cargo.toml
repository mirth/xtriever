[package]
name = "xtriever-analysis"
description = "Xtriever: text analysis — chunking (pure Rust, std-only)"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
xtriever-core = { path = "../xtriever-core" }

[lints]
workspace = true

[dev-dependencies]
proptest = "1.11.0"
serde = { workspace = true, features = ["derive"] }
serde_json = "1.0.151"
sha2 = "0.11.0"
