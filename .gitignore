target/

# Python reference generator (Constitution §II goldens) — see reference/
reference/.venv-*/
__pycache__/
*.py[cod]

# Pinned model weights: 87.1 MiB, downloaded by gen_001_fixtures.py, NEVER committed.
# reference/fixtures/ IS tracked — those are the goldens.
reference/models/
*.safetensors

# uniffi bindgen output (quickstart Step 3)
/build/

# macOS / Xcode (harness/ios lands in PR 2)
.DS_Store
DerivedData/
xcuserdata/
*.xcuserstate
*.xcarchive
*.ipa

# iOS harness build inputs — regenerate with scripts/build-ios-harness.sh.
# The XCFramework alone is ~262 MB and the Swift bindings are generated from the Rust.
harness/ios/XtrieverSpike/Frameworks/
harness/ios/XtrieverSpike/Sources/XtrieverSpike/Generated/
harness/ios/XtrieverSpike/.build/
harness/ios/XtrieverSpike/Sources/XtrieverSpike/XtrieverData/
harness/ios/XtrieverSpikeApp/XtrieverSpikeApp.xcodeproj/

# BEIR evaluation datasets: ~23 MB fetched by scripts/fetch-beir.sh, pinned by hash in
# reference/datasets/beir-manifest.json, never committed (Feature 003 FR-008).
reference/datasets/beir/
