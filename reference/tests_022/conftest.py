"""Feature 022 checks: the join, the splitters' bounding and merging, MaxP, the run files and
the anchor comparison, the decision rule. Red until `chunking_study` exists."""

import helpers_022  # noqa: F401  (puts reference/ on sys.path)
