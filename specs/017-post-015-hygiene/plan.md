# Implementation Plan: Hygiene after 013–016

**Branch**: `017-post-015-hygiene` | **Date**: 2026-09-16 | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `/specs/017-post-015-hygiene/spec.md`

## Summary

Four small obligations left by 013–016, done together: (1) the Wikipedia demo's host goldens
regenerated by the current engine (depths 0 / 5 / **10** / 20 — the generator and the device
harness both gain depth 10) and one device run with parity, footprint and per-depth latency,
committed as records (1 thread and default threads); (2) the FFI budget test rewritten to assert
the contract (no error, the time-limit flag, scored first, at least one partial re-rank; with no
budget nothing skipped and everything re-ranked) with no wall-clock bound — the budget found per
query by probing verified endpoints and bisecting, re-measured at every step; (3) the reference suites made
collectable together via per-suite helper modules; (4) the iOS demo README and the CLI wiki
docs naming the re-rank default, the upgrade behaviour and the override. No engine, format,
default or ranking change.

## Technical Context

**Language/Version**: Rust 1.91.1 (one example + one test file), Swift 6 (the harness constant, two test-support edits), Python 3.12 (test helpers), Markdown

**Primary Dependencies**: unchanged; the two pinned models, the 008 Wikipedia index artefact (`target/xt-wiki/`), the iPhone

**Storage**: `target/xt-wiki/expected.json` regenerated (git-ignored, staged into the package); the device record under `specs/017-post-015-hygiene/runs/`

**Testing**: `cargo nextest run -p xtriever-ffi --run-ignored all -E 'test(budget)'` on this machine; the device harness on the phone (owner); `pytest reference/tests_012 reference/tests_014 reference/tests_016 -q`

**Target Platform**: host + the iPhone (device run); the cross-target checks unchanged

**Project Type**: maintenance (tests, goldens, docs, one record)

**Performance Goals**: none new; the record reports memory and latency under the 600 MB ceiling (ADR-0010)

**Constraints**: nothing ranking-affecting; no baseline change; device and team identifiers only on the command line

**Scale/Scope**: ~40 lines Rust, ~10 Swift, ~30 Python, ~30 Markdown, one JSON record

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

Authority: `.specify/memory/constitution.md` (v1.4.0, ratified 2026-09-10, last amended 2026-09-13). Every row below MUST be
marked **PASS**, **FAIL**, or **N/A** with a one-line justification — an empty verdict is a FAIL.

- Any **FAIL** blocks the phase. Do not proceed, and do not weaken the gate to make it pass.
- Any **N/A** MUST state why the principle cannot apply to this feature.
- A violation that is genuinely required is recorded in Complexity Tracking below and REQUIRES an
  accepted ADR in `docs/adr/`; the row stays **FAIL** until that ADR is merged.
- These are plan-time gates. They do not replace the blocking CI gates in the constitution's
  "Quality Gates (CI)" table.

### Core Principles

| # | Principle | Gate | Verdict | Justification |
|---|---|---|---|---|
| I | Reuse Before Build | Commodity components come from `tantivy` (inverted index/BM25), `tokenizers`, `candle`, `roaring`. No hand-written inverted index, ANN graph, or tensor runtime without an accepted ADR showing the reused crate fails a *measured* requirement. | **PASS** | Nothing built. |
| II | Executable Oracles Over Prose (NON-NEGOTIABLE) | Acceptance tests are written and committed failing before implementation. Behaviour with a reference implementation is pinned to golden fixtures from `reference/` with tolerances stated in the spec. Ranking-affecting work reports nDCG@10 / Recall@100 deltas from `xtriever-eval` on SciFact / NFCorpus / FiQA. Invariants (analyzer determinism, index round-trips, filter algebra) are property-tested. | **PASS** | The budget test's new assertions are committed first (red against the old behaviour only in the sense that the file changes; the mutation check is recorded); the device record is the executable evidence for the goldens; no ranking change, so no deltas — stated. |
| III | Portability Is a Feature | `xtriever-core`, `-analysis`, `-pipeline`, `-ltr`, `-eval` stay pure Rust and `std`-only: no C/C++ build deps, no `async`, no `tokio`, no unconditional threads, no `std::time::Instant` in library code. C/C++ deps live only in `xtriever-dense`, `-rerank`, `-ffi` behind non-default features enforced by `deny.toml`. `cargo check` passes on host, `aarch64-apple-ios`, `aarch64-apple-ios-sim`, `aarch64-linux-android` (wasm32 best-effort). On-device RSS stays under the spec's ceiling (default 600 MB for the full pipeline — 100k-chunk hybrid index with embedder and cross-encoder loaded, ADR-0010); a smaller measured configuration says so and claims nothing for the reference one. | **PASS** | No crate code changes; the device run re-measures the footprint under the 600 MB ceiling and records it. |
| IV | Measured, Not Asserted | Every performance claim is backed by a `criterion` benchmark against budgets stated in the spec (p50/p99 latency, index size, RSS). Benches and eval runs are reproducible: fixed seeds, model revisions pinned by SHA, pinned dataset versions. `explain()` reports per-stage scores and features for every hit. | **PASS** | The whole point of item (1): the device record with parity, memory and per-depth latency, including depth 10 for the 014 finding. |
| V | Small, Explicit Interfaces | The `xtriever-core` traits (`Analyzer`, `LexicalIndex`, `Embedder`, `VectorIndex`, `Reranker`, `Ranker`), the on-disk format, and error semantics are unchanged — or the change has human review plus an ADR in `docs/adr/`. Core APIs are synchronous; async wrappers only in `xtriever-ffi` or server crates. Internal `DocId` is a dense `u32` and backends never see external string ids. One crate per stage, dependencies pointing downward only: `core` ← stage crates ← `pipeline` ← `ffi`/`cli`. | **PASS** | No interface, format or default change. |
| VI | Graceful Degradation and Determinism | Failure or timeout of an ML stage degrades to the previous stage's results, never to an error, unless the caller opted into strict mode. Same index + same query + same config yields identical results, ties broken by ascending `DocId`. Indexes carry the embedder fingerprint and a format version; mismatches are hard errors at open time, never silent. | **PASS** | The budget test asserts degradation's contract more precisely and removes the machine-dependent assertion. |
| VII | Rust Hygiene | Edition 2024, toolchain pinned in `rust-toolchain.toml`, `Cargo.lock` committed, dependencies added with `cargo add` at current versions — never from memory. `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo nextest run`, `cargo deny check` all pass. No `unwrap`/`expect`/`panic!`/`todo!` in library code; library errors are `thiserror` enums, `anyhow` only in binaries. Hand-written `unsafe` only in `xtriever-dense` and `xtriever-rerank`, and there only in SIMD kernels and in read-only memory mapping behind a non-default feature; each block preceded by `// SAFETY:` and tested against the safe path (ADR-0007, ADR-0009). `xtriever-ffi` may carry a crate-level `#![allow(unsafe_code)]` for uniffi scaffolding, hand-written modules re-declaring `#![deny(unsafe_code)]` (ADR-0003). All public items documented (`missing_docs` on). | **PASS** | Test and example code only; the gate runs unchanged. |

### Agent Operating Rules

| # | Rule | Gate | Verdict | Justification |
|---|---|---|---|---|
| 1 | Never invent an API | Every external crate item this plan relies on was read from the docs for the pinned version and is cited here by item path. | **PASS** | `wiki expected` (`cli/src/wiki/expected.rs:21–35`), the harness (`DeviceMeasurementTests.swift:22, 204–250, 275`), `extract-device-run.py`, `budget.rs:32–60`, pytest's import mode behaviour observed in 016 — research D1–D3. |
| 2 | Don't touch the contract uninvited | This plan modifies `xtriever-core` traits or `deny.toml` only if the spec explicitly says so; otherwise it stops and asks. | **PASS** | `xtriever-core` and `deny.toml` untouched. |
| 3 | One spec, small PRs | Work is scoped to this spec on its own branch, and the planned change stays under ~800 changed lines — or the plan states the split. | **PASS** | ~110 lines plus one record; one PR. |
| 4 | Tests first | Task ordering puts the acceptance tests first, committed failing, ahead of any implementation task. | **PASS** | The budget test rewrite is the test; the helper-module change is verified by the combined pytest run; the goldens are verified by the device run. |
| 5 | Full gate before done | The plan budgets for running the whole local gate and pasting eval/bench deltas into the PR before any task is called done. | **PASS** | Quickstart Step 4: the Rust gate, the model-backed FFI budget test, the combined pytest, the Swift suite on the simulator (owner), the device run (owner), byte-identity of every baseline. |
| 6 | Never weaken the oracle | On a metric drop or a target that fails to build, the plan's response is to stop and report — never to relax tests, tolerances, or thresholds. | **PASS** | The elapsed-time assertion is removed because it measures the machine, not the contract — and the contract assertions are strengthened (the generous-budget negative). Parity tolerance and the footprint ceiling are untouched. |
| 7 | Prefer boring code | No macros for their own sake, no trait gymnastics, no premature generics. | **PASS** | A constant, a loop over budgets, two helper modules, two paragraphs. |

**Initial gate (pre-Phase 0)**: PASS — 2026-09-16, Claude (plan author).

**Post-design gate (post-Phase 1)**: PASS — 2026-09-16, Claude, after research D1–D5.

## Project Structure

### Documentation (this feature)

```text
specs/017-post-015-hygiene/
├── plan.md · research.md · data-model.md · quickstart.md
├── runs/<device>-<timestamp>-mmap-threadsdefault.json   # the device record (owner's run)
├── report.md · pr-description.md
└── tasks.md
```

### Source Code (repository root)

```text
crates/xtriever-cli/src/wiki/expected.rs                 # depths 0 / 5 / 10 / 20
crates/xtriever-cli/src/wiki/mod.rs                      # re-rank default paragraph in the command docs
crates/xtriever-ffi/tests/budget.rs                      # the contract assertions; no elapsed bound
swift/Xtriever/Tests/XtrieverTests/DeviceMeasurementTests.swift   # depths [0, 5, 10, 20]
reference/tests_012/{helpers_012.py,conftest.py,test_*.py}, reference/tests_014/{helpers_014.py,conftest.py,test_*.py}
apps/ios-wiki-demo/README.md
target/xt-wiki/expected.json                              # regenerated (git-ignored)
```

**Structure Decision**: no library code; a test, an example, a Swift test constant, test
helpers, docs and a record.

## Complexity Tracking

None.
