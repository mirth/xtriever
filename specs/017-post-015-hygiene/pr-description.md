# 017 hygiene after 013–016

Four obligations left by the last four features, closed and recorded. No engine, format,
default or ranking change.

1. **The Wikipedia demo's host goldens** predated 015's re-rank default. Regenerated by
   `xtriever wiki expected` from the current engine (depth 10 added to the generator and to the
   device harness): depth-0 responses identical 20/20; 18/20 depth-5 and 20/20 depth-20 heads
   changed under the interpolating rule. **Two device records** (iPhone17,5, mmap; 1 thread and
   default threads) committed under `runs/`: parity **PASS at 0 / 5 / 10 / 20** (lexical
   bit-identical, fused order identical, re-rank within 4.8 × 10⁻⁶), peak **307 MB** vs the
   600 MB ceiling (008 on the same phone: 536 MB — 010's id-map saving on a Wikipedia record),
   per cross-encoder pair 94 ms (008: 97) — the new rule costs nothing on the phone. Depth 10:
   **1.41 s** median vs 2.31 s at depth 20 (default threads), for −0.3 mean nDCG@10 (014) —
   measured, not adopted.
2. **The FFI budget test** asserted `elapsed_ms < 1000` and failed on this laptop on `main`
   (`q0: 3414 ms`). Fixed budgets are machine-dependent in both directions (a slow machine
   never reaches the re-ranker at 200 ms; a fast one finishes inside it), and so was a
   "generous" 60 s budget under the contended suite. The budget is now bisected per query
   between an unbudgeted depth-0 search and an unbudgeted full search, re-measured at every
   step, and the assertions are the contract only: no error, the time-limit flag, scored hits
   first, at least one partial re-rank, and — with no budget — nothing skipped, everything
   re-ranked. Passes alone and inside the full model-backed suite (22/22); the mutation check
   (re-ranker ignoring its remaining budget) fails it.
3. **The reference suites** collect in one run: `pytest reference/tests_012 reference/tests_014
   reference/tests_016` → 88 passed (per-suite helper modules; no test imports `conftest` by
   name).
4. **Docs**: the iOS demo README and the CLI `wiki` docs state the default, the upgrade
   behaviour (ADR-0012) and the `Replace` override.

Changes under `crates/`: `xtriever-cli/src/wiki/{expected,mod}.rs` (depth 10; docs) and
`xtriever-ffi/tests/budget.rs`. Unchanged: every baseline and cell, the pipeline, the models.

Gate: fmt · clippy 0 · nextest workspace · deny · iOS / iOS-sim / Android checks · the
model-backed FFI suite · pytest 88/88 · no-stubs · no identifiers in the tree · the Swift
simulator suite (owner).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
