# 015 interpolated re-ranking — the cross-encoder informs the fused order, and that is the default

**The rule** (014's winner under a rule fixed before the runs): within the re-ranked head,
`0.5 · minmax(fused score) + 0.5 · minmax(cross-encoder score)`, ties by fused rank; the rest
in fused order. Replace-order (Feature 006) stays selectable as `RerankMode::Replace`, bit for
bit.

## Numbers (nDCG@10; Recall@100 unchanged everywhere; every run verified by the 003 reference)

| dataset | hybrid-rerank-v2 (replace) | **hybrid-rerank-v3** (interpolate) | 014's `lin-0.5-d20` cell | Δ v2 → v3 |
|---|---|---|---|---|
| scifact | 0.695430 | **0.720711** | 0.720711 | **+0.025280** |
| nfcorpus | 0.360854 | **0.362246** | 0.362246 | +0.001392 |
| fiqa | 0.374214 | **0.390964** | 0.390964 | **+0.016750** |
| mean | 0.476833 | **0.491307** | 0.491307 | **+0.014474** |

`hybrid-rerank-v3` equals 014's derived runs **list for list** on all three sets (`check-cell:
PASS (per-query metrics, lists)`) — the engine's rule is the derivation that won. Same 20
cross-encoder calls per query (6,000 / 6,460 / 12,960 pairs, as 013). `hybrid-rerank-v2` still
reproduces 0.695430 / 0.955000.

## What a caller sees

- **Default**: `HybridConfig::new` / `IndexConfig(rerank_mode=None)` → `Interpolate { alpha: 0.5 }`;
  `info().rerank_mode` reports it.
- **Override**: `SearchOptions.rerank_mode` (Rust, Swift, Python: `xtriever.RerankMode.REPLACE()`)
  per search; `IndexConfig.rerank_mode` at build. α outside `[0, 1]` → `Schema` error.
- **Explain**: `rerank_combined` (the ordering score; `None` under `Replace`), feature
  `rerank.combined` — eight features now.

## The descriptor and existing indexes (ADR-0012, owner's Q1 = A)

`rerank_mode` is written after `rerank_depth`; **a descriptor without the key reads as the
default**, format version unchanged. So the shipped Wikipedia index and the 007 fixture index
gain the new order on upgrade with no rebuild; the old order is one override away. Tests:
`descriptor_without_rerank_mode_reads_as_interpolate_half`, `descriptor_round_trips_rerank_mode`.
The wiki demo's local goldens (`target/xt-wiki/expected.json`, git-ignored) follow the engine
and must be regenerated (`xtriever wiki expected`) before the next device run.

## Goldens

- `reference/fixtures/006/pipeline_order.json`: the 006 reference generator gained the
  interpolating rule and 11 `interpolate_cases`; the existing `cases` array is byte-identical
  (`--order-only` flag; `manifest.json` refreshed). Rust golden test `every_interpolation_case_is_exact`.
- `swift/Xtriever/Tests/Fixtures/expected.json` regenerated by the existing 007 generator:
  **`without_reranker` identical 8/8**; `with_reranker` heads changed for 7 of 8 queries;
  `info` gains `rerank_mode`; hits gain `rerank_combined_bits`.

## Tests first (committed red)

Pipeline (`rerank_golden`, `rerank_prop`, `persist`, `rerank`, `explain`), eval (`rerank_run`),
FFI (`options`, `parity`), Python (`test_surface`, `test_options`, `test_search`), Swift
(`InfoTests`, `ParityTests`, `Support`). The five 006 tests that assert the replace order now
select `RerankMode::Replace` explicitly (`support::replace_options`) — their assertions are
unchanged.

## Size (Rule 3)

~1,180 changed lines excluding the regenerated goldens, the three baselines, the spec
documents and the ADR — over the ~800 guideline. Of these ≈ 640 are tests and ≈ 70 the
reference generator; production code ≈ 330 (pipeline 210, FFI 60, eval 35, CLI 5, Python 25).
If a split is preferred: **PR A** pipeline + 006 oracle + pipeline tests + ADR (≈ 700), **PR B**
eval v3 + FFI/Python/Swift surfaces + regenerated 007 goldens + baselines + docs (≈ 480); B
depends on A.

## Unchanged

`xtriever-core`, `deny.toml`, every 003–014 baseline and the 014 cells (byte-identical), the
006 `cases`, the format version, the models.

Gate: fmt · clippy workspace (0) · nextest 278/278 · deny · iOS / iOS-sim / Android checks ·
pytest 014 57/57 · wheel + pytest python 33/33 (models) · FFI model-backed suite · Swift suite
on the simulator (owner) · no-stubs · no identifiers.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
