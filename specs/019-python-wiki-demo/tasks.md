# Tasks: The Python Wikipedia Demo

**Input**: Design documents from `/specs/019-python-wiki-demo/`

**Prerequisites**: [plan.md](./plan.md), [spec.md](./spec.md), [research.md](./research.md)
(D1–D18), [data-model.md](./data-model.md), [contracts/cli.md](./contracts/cli.md),
[contracts/records.md](./contracts/records.md), [quickstart.md](./quickstart.md); on disk: the
two models, the 007 fixture index + goldens, the 008 artefact at `target/xt-wiki/` with
`expected.json` (017), the snapshot `reference/datasets/wiki/simple.jsonl`, the wheel under
`target/wheels/`, `uv`.

**Tests**: **Mandatory** (Principle II; spec FR-017): each PR opens with its tests committed
red. Two PRs on this branch (plan, Rule 3): **PR A** = Phases 1–6 (search, about, measure,
the host record; commits **A1** = red, **A2** = green + record + docs); **PR B** = Phase 7
(build, slice parity; commits **B1** = red, **B2** = green + record + docs) — PR B is opened
after PR A merges (one branch, `019-python-wiki-demo`, rebased onto `main`). Phase 8 closes
each PR.

**Organization**: Setup → Foundational (skeleton, conftest, PR A red tests) → US1 → US2 →
US4 → US5 → PR A gate → US3 (PR B) → Polish. US3 (P1) is scheduled after US4/US5 (P2)
because it is the second PR — the priority order holds within each PR.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: parallelizable (different files, no dependency on an incomplete task)
- **[Story]**: US1–US5 from spec.md. **Rule 6 stop-points are marked ⛔.**

## Path Conventions

`apps/python-wiki-demo/{pyproject.toml,README.md,.gitignore}`, `apps/python-wiki-demo/wikidemo/*.py`,
`apps/python-wiki-demo/tests/*.py`; `specs/019-python-wiki-demo/{runs/,report.md,pr-description.md}`;
pointers in `README.md`, `apps/ios-wiki-demo/README.md`, `specs/009-ios-wiki-demo/{spec,report}.md`.
All commands from the repository root with `unset SDKROOT`; `PY=apps/python-wiki-demo/.venv/bin`.

---

## Phase 1: Setup

- [ ] T001 Confirm the inputs (quickstart Prerequisites): `ls reference/models/all-MiniLM-L6-v2/model.safetensors reference/models/ms-marco-MiniLM-L-6-v2/model.safetensors target/xt-wiki/index/xtriever-pipeline.json target/xt-wiki/expected.json target/xt-wiki/ATTRIBUTION.txt reference/datasets/wiki/simple.jsonl swift/Xtriever/Tests/Fixtures/index/xtriever-pipeline.json swift/Xtriever/Tests/Fixtures/expected.json target/wheels/xtriever-*.whl`; `jq '.queries | length, (.queries[0].depths | keys)' target/xt-wiki/expected.json` → 20, `["0","10","20","5"]`; `mkdir -p specs/019-python-wiki-demo/runs`
- [ ] T002 Create `apps/python-wiki-demo/pyproject.toml` (research D15): `[project] name = "xtriever-wiki-demo"`, `version = "0.1.0"`, `requires-python = ">=3.12"`, `dependencies = ["xtriever>=0.1.0", "tokenizers==0.23.2"]` (the version from `crates/xtriever-dense/Cargo.toml:23` — cite it in a comment), `[project.optional-dependencies] test = ["pytest>=8"]`, `[project.scripts] wikidemo = "wikidemo.cli:main"`, `[build-system] requires = ["setuptools>=68"]`, `[tool.setuptools] packages = ["wikidemo"]`, `[tool.pytest.ini_options] testpaths = ["tests"]`, `markers = ["models: needs the pinned models and the 007 fixture index on disk (skipped otherwise)"]`; `apps/python-wiki-demo/.gitignore` with `.venv/` and `__pycache__/`; `apps/python-wiki-demo/wikidemo/__init__.py` with the docstring (what the demo is, what it is not — no retrieval logic), `DEFAULT_DEPTH = 10`, `DEPTHS = (0, 5, 10, 20)`, `DEFAULT_K = 10`; `wikidemo/__main__.py` (`from .cli import main; raise SystemExit(main())`); an empty-bodied `wikidemo/cli.py` with `def main(argv=None) -> int` returning 2 with "not implemented" — enough for the script entry to resolve
- [ ] T003 Quickstart Step 0: `cd apps/python-wiki-demo && uv venv .venv --python 3.12 && uv pip install --python .venv/bin/python ../../target/wheels/xtriever-*.whl -e ".[test]"`; `.venv/bin/python -c "import xtriever, tokenizers; print(xtriever.__version__, tokenizers.__version__)"` → `0.1.0 0.23.2`; `.venv/bin/wikidemo --help` runs (exit 2 is fine at this point)

---

## Phase 2: Foundational — the shared test support and PR A's red tests

- [ ] T004 Write `apps/python-wiki-demo/tests/conftest.py` on the 011 pattern (`python/tests/conftest.py`): `REPO` (parents[3]), `FIXTURE_INDEX`, `FIXTURE_GOLDENS` (the 007 files), `EMBEDDER`/`RERANKER` from the same environment variables, `WIKI_ARTEFACT = REPO / "target/xt-wiki"`, `CHUNK_A`/`CHUNK_B` (`reference/fixtures/008/`), `QUERIES_008`; `missing_for_models()` and the `pytest_collection_modifyitems` skip-with-reason hook; `f64_bits`/`f32_bits`; fixtures `fixture_handle` (opened with both models, mmap) and `fixture_goldens`; a `run_cli(argv, env) -> (code, stdout, stderr)` helper calling `wikidemo.cli.main` with captured streams
- [ ] T005 [P] Write `apps/python-wiki-demo/tests/test_hits.py` (research D3, D4): `test_title_and_passage_splits_at_first_blank_line` (`"T\n\nbody\n\nmore"` → `("T", "body\n\nmore")`; no blank line → `None`); `test_wikipedia_url_matches_the_cli_cases` — the eleven `(title, want)` pairs from `crates/xtriever-cli/src/wiki/url.rs:33-45` verbatim; `test_marks_follow_the_ios_rule` — fused `[a,b,c,d]`, re-ranked `[b,a,e,c]` → `b: up(1)`, `a: down(1)`, `e: new`, `c: same`, dropped `[d]`; identical lists → all `same`, none dropped; `test_features_are_the_eight_engine_names_in_order` — `FEATURE_NAMES == ["bm25.score","bm25.rank","dense.score","dense.rank","fused.score","rerank.score","rerank.rank","rerank.combined"]` and `features(explain)` yields `None` → rendered "not seen by this stage"; `test_displayed_hit_without_title_line_uses_the_id` (a stub hit object with `text="plain"`, `chunk=None` → title = external id, url None, ordinal None)
- [ ] T006 [P] Write `apps/python-wiki-demo/tests/test_record.py` (D9): `test_canonical_json_matches_the_rust_rule` (`{"b":1,"a":"é"}` → `{"a":"é","b":1}`); `test_identity_of_the_shipped_corpus` — the basis from `contracts/records.md` with the shipped `snapshot`, `exclusions`, `chunker` and the fingerprint string (constants in the test) → `ea0fc78c4dce30cebf066033327cc7a442385c605be8c67eadb772329eab2027`; with `partial=2000` a different hash; `test_attribution_text_shape` — four lines, the manifest's snapshot date, source and licence URL, the identity and the timestamp; `test_machine_name_is_a_hardware_model_not_a_hostname` (`machine_name()` contains no `platform.node()` and no `/`)
- [ ] T007 [P] Write `apps/python-wiki-demo/tests/test_inputs.py` (D11): `test_defaults_resolve_against_the_repo_root`; `test_flag_beats_env_beats_default` for `artefact`, `embedder`, `reranker`; `test_missing_input_names_the_producer` — for each of artefact (`target/xt-wiki/index/xtriever-pipeline.json`), embedder, re-ranker, snapshot, expected, queries: `first_missing(paths, needs)` returns `(what, path, producer)` with the producer strings of `contracts/cli.md`'s table (`scripts/fetch-model.sh`, `scripts/fetch-model.sh --manifest reference/models/manifest-rerank.json`, `scripts/fetch-wiki.sh`, `wiki build`, `wiki expected`); `test_cli_exits_1_on_a_missing_artefact` via `run_cli(["about","--artefact","/nonexistent"])` → code 1, stderr starts `wikidemo: missing`, and no model was loaded (the call returns in < 1 s)
- [ ] T008 [P] Write `apps/python-wiki-demo/tests/test_render.py` (D16, contracts/cli.md): using stub response objects (dataclasses mimicking `Hit`/`StageReport` attributes): `test_open_line`, `test_fused_list_lines` (rank, title, id, `passage N`, URL line present only with a title line, passage indented), `test_reranked_list_has_mark_column_and_dropped_line`, `test_explain_block_prints_eight_features`, `test_stage_line_variants` (dense skipped with reason; re-rank skipped; degraded stage + reason; time limit ignored yes/no), `test_wall_line_and_footprint`, `test_empty_result_line` (`no passages found for "…"`), `test_snippet_cuts_and_says_so`
- [ ] T009 [P] Write `apps/python-wiki-demo/tests/test_search.py` (`pytestmark = models`, spec SC-002): `test_fused_list_equals_the_goldens_depth_zero` — for every 007 golden query, `run_search(query, k, depth=0)` hits' `(external_id, f64_bits(score), f32_bits(bm25), f32_bits(dense))` equal `without_reranker.hits`; `test_reranked_list_equals_the_goldens` — `run_search(query, k, depth=q["rerank_depth"])` second-call hits equal `with_reranker.hits` on ids, `score_bits`, `rerank_score_bits`, `rerank_rank`, `rerank_combined_bits`; `test_marks_are_computed_between_the_two_calls` (marks keyed by every re-ranked id; dropped ⊆ fused ids); `test_cli_prints_the_engine_values` — `run_cli(["search","--artefact",<fixture artefact dir>,"--explain","-k","5",q0.text])` → code 0, stdout contains each golden id and the `fused.score:` lines with the formatted values; `test_depth_zero_is_one_call`; `test_strict_budget_error_exits_1` (`--budget-ms 1 --strict` → code 1, stderr `wikidemo: BudgetExhausted` or whichever kind the engine raises — assert the class name printed equals `type(e).__name__` of a direct `xtriever` call with the same options); `test_no_hits_exits_0` (a query of only stop-words that yields no hits on the fixture — pick one by trying against the fixture at test time, skip if every candidate has hits). The fixture "artefact" for the CLI: a tmp dir with `index` → symlink to `swift/Xtriever/Tests/Fixtures/index` and an `ATTRIBUTION.txt` stub — the CLI must accept an index without `corpus.json` (about prints "no corpus sidecar" for those fields)
- [ ] T010 [P] Write `apps/python-wiki-demo/tests/test_about.py` (`models`): `test_about_fields_equal_info_and_sidecar` — against `target/xt-wiki` when present (else the fixture artefact, sidecar fields marked absent): documents, format version, fingerprints, candidate depth, rrf k, `re-rank depth (engine default): <info.rerank_depth>`, `re-rank depth (demo default): 10`, mode; `test_attribution_verbatim` — the About output's attribution block equals `ATTRIBUTION.txt` byte for byte
- [ ] T011 [P] Write `apps/python-wiki-demo/tests/test_measure.py` (D13): on synthetic truths and responses (dataclass stubs): `test_pass_when_everything_matches`; `test_incomplete_on_missing_query`, `…_on_depth_set_mismatch` (goldens with depths {0,5,20} vs measured {0,5,10,20}), `…_on_hit_count`, `…_on_unknown_id`, `…_on_score_present_one_side_only`; `test_lexical_bits_must_be_exact` (one bm25 bit off → `lexicalBitIdentical` 19/20, verdict FAIL); `test_fused_order_only_at_depth_zero` (swapped neighbours at depth 10 within tolerance → PASS; at depth 0 → FAIL); `test_tolerance_is_1e_3` (dense diff 9e-4 PASS, 2e-3 FAIL); `test_all_bits_identical_count`; `test_medians_and_latency_keys` (per-depth medians/maxima; `medianRerankedMs` = depth 10, `medianRerankedAtEngineDefaultMs` = depth 20, total = 0 + 10 per query); `test_record_has_no_hostname` and the key set of `contracts/records.md`; `models`: `test_fixture_goldens_pass` — the 007 goldens reshaped to the truth shape (one depth per query) through the same comparison → PASS
- [ ] T012 Quickstart Step 1: `$PY/pytest apps/python-wiki-demo/tests -q` → every file errors on import (`wikidemo.hits` … do not exist); paste the count into `specs/019-python-wiki-demo/report.md` ("Red checkpoint A"). **⛔ Commit A1**: the skeleton, the tests, the report stub

---

## Phase 3: User Story 1 — A person searches Wikipedia from the command line and gets passages (Priority: P1)

**Goal**: `wikidemo search "…"` prints the opened line, the ranked passages with title / id / position / URL / text, exit codes per the contract.

**Independent Test**: quickstart Step 3's first command and the missing-input command.

- [ ] T013 [P] [US1] `apps/python-wiki-demo/wikidemo/inputs.py` (D11): `repo_root()` (walk up from `__file__` to the directory holding `Cargo.toml` and `.specify/`); `@dataclass Paths` with `artefact, index_dir, attribution, corpus_json, expected, embedder, reranker, snapshot, manifest, queries`; `resolve(args) -> Paths` (flag → env `XTRIEVER_WIKI_ARTEFACT` / `XTRIEVER_MODEL_DIR` / `XTRIEVER_RERANK_MODEL_DIR` → default, relative to the root); `PRODUCERS` table (the contract's strings); `first_missing(paths, needs: list[str]) -> tuple[str, Path, str] | None` checking the sentinel file per input (`index/xtriever-pipeline.json`, `model.safetensors`, `simple.jsonl`, `expected.json`, `queries.json`); `class MissingInput(Exception)`; `require(paths, needs)` raising it
- [ ] T014 [P] [US1] `apps/python-wiki-demo/wikidemo/hits.py` (D3, D4): `title_and_passage(text) -> tuple[str, str] | None`; `wikipedia_url(title) -> str` (the byte loop: `A–Z a–z 0–9 - _ . ~ /` kept, else `%XX` upper-case); `FEATURE_NAMES`; `features(explain) -> list[tuple[str, float | None]]` (`bm25_score, bm25_rank, dense_score, dense_rank, fused, rerank_score, rerank_rank, rerank_combined`); `@dataclass Mark(kind: str, n: int = 0)` with `render()` → `↑n` / `↓n` / `=` / `new`; `marks(fused_ids, reranked_ids) -> tuple[dict[str, Mark], list[tuple[str, int]]]` (dropped with their fused rank); `@dataclass DisplayedHit(rank, external_id, title, passage, url, parent, ordinal, score, rerank_score, mark, features)` and `displayed(hits, marks=None, explain=False) -> list[DisplayedHit]`
- [ ] T015 [P] [US1] `apps/python-wiki-demo/wikidemo/record.py` (D9, D12): `canonical_json(value)`; `corpus_identity(snapshot, exclusions, chunker, embedder_fingerprint, partial=None)`; `CHUNKER = {"version": 1, "budget": "256 - token_count(title)", "cost": "MiniLmEmbedder::token_count(unit) - 2"}`; `attribution_text(manifest, identity, recorded_at)`; `now_rfc3339()`; `machine_name()` (`sysctl -n hw.model` via `subprocess` on macOS, else `platform.machine()`; never `platform.node()`); `os_name()`; `peak_resident_bytes()` (`ru_maxrss`, ×1024 on Linux); `threads()` → `(n, source)` from `RAYON_NUM_THREADS` or `os.cpu_count()`; `write_json(path, obj)` (two-space indent, `ensure_ascii=False`, trailing newline)
- [ ] T016 [US1] `apps/python-wiki-demo/wikidemo/search.py` (D2): `@dataclass Opened(handle, paths, info, open_ms)`; `open_artefact(paths, need_reranker=True) -> Opened` (wall clock around `xtriever.IndexHandle.open(str(index_dir), str(embedder), str(reranker), xtriever.LoadPath.MMAP)`); `@dataclass StageRun(label, options, response, wall_ms, peak_bytes)`; `run_search(opened, query, k, depth, budget_ms, strict, mode) -> list[StageRun]` — first `SearchOptions(k=k, rerank_depth=0, max_time_ms=budget_ms, strict=strict, explain=True)`, then, if `depth > 0`, `SearchOptions(k=k, rerank_depth=depth, rerank_mode=(xtriever.RerankMode.REPLACE() if mode == "replace" else None), …)`; `mode_label(opened.info, mode)` → `"interpolate α 0.5"` from `info.rerank_mode` or `"replace"`
- [ ] T017 [US1] `apps/python-wiki-demo/wikidemo/render.py` (D16, contracts/cli.md — the exact lines): `open_line(opened)`; `WARMUP_LINE`; `list_block(label, hits: list[DisplayedHit], wall_ms, snippet=None, explain=False) -> list[str]` (header `<label>, <n> hits, <wall> ms[, passages cut to N characters]`; hit line `f"{rank:2d}. {mark:4} {title}  {id}  passage {ordinal+1}"` with the mark column only when marks exist; URL line when present; the passage indented four spaces; eight `    name: value` lines with `--explain`, `None` → `not seen by this stage`, floats `%.4f`, ranks as ints); `dropped_line(dropped)`; `stage_line(stages, elapsed_ms)`; `wall_line(fused_ms, reranked_ms, peak_bytes)`; `empty_line(query)`; `error_line(exc)` → `wikidemo: <type name>: <message>`
- [ ] T018 [US1] `apps/python-wiki-demo/wikidemo/cli.py`: `build_parser()` with the common flags on every subcommand (`--artefact`, `--embedder`, `--reranker`) and `search` per the contract (`-k` int default 10; `--depth` `type=int, choices=DEPTHS, default=DEFAULT_DEPTH`, help naming the engine's 20 and the 018 numbers "−0.3 mean nDCG@10 on the BEIR sets for half the cross-encoder calls"; `--budget-ms`; `--strict`; `--mode` choices; `--explain`; `--snippet` int; `query`); `main(argv=None) -> int`: resolve paths → `require` (US1's needs: artefact, embedder, reranker) → `MissingInput` → the two-line stderr message, return 1 → `cmd_search` → `xtriever.XtrieverError` → `error_line`, return 1; `cmd_search` prints the open line, the warm-up line, the fused block, the re-ranked block with marks and the dropped line, the stage line of the last call, the wall line; no hits → `empty_line`, return 0
- [ ] T019 [US1] `$PY/pytest apps/python-wiki-demo/tests/test_hits.py tests/test_inputs.py tests/test_render.py tests/test_record.py -q` (paths under `apps/python-wiki-demo/`) → green; `$PY/pytest apps/python-wiki-demo/tests/test_search.py -q` → green (the goldens on bits); quickstart Step 3 lines 1, 3, 6, 7, 8 by hand — paste the `search "why is the sky blue"` output into the report

---

## Phase 4: User Story 2 — The person sees the pipeline work (Priority: P1)

**Goal**: marks, explanations, the stage report, flags reflected, warm-up label, wall and footprint.

**Independent Test**: quickstart Step 3 lines 2, 4, 5.

- [ ] T020 [US2] Verify US2's acceptance on the shipped index and record it: `search --explain --depth 20 "who painted the Mona Lisa"` (eight features per hit, `rerank.combined` present on the head), `search --budget-ms 300 "what is the capital of Australia"` (a `degraded:` or `re-rank skipped` term in the stage line, exit 0), the same with `--strict` (exit 1, the engine's error kind), `--mode replace` (the re-ranked head ordered by `rerank.score`; `rerank.combined` "not seen"), `--depth 0` (one block, no marks); paste the stage lines into `specs/019-python-wiki-demo/report.md`. Fix anything the contract says differently (**⛔** only rendering — never the engine's values)

---

## Phase 5: User Story 4 — The person sees what they are searching and on what terms (Priority: P2)

- [ ] T021 [US4] `apps/python-wiki-demo/wikidemo/about.py`: `read_sidecar(paths) -> dict | None`; `read_attribution(paths) -> str | None`; `about_lines(opened, sidecar, attribution, manifest_licence_url) -> list[str]` in the contract's order, with `re-rank depth (engine default): <info.rerank_depth>` and `re-rank depth (demo default): 10` on adjacent lines, sidecar-derived lines reading `(no corpus sidecar)` when absent, then a blank line, the attribution verbatim, the licence URL; `cli.py` gains `about` (needs: artefact, embedder, reranker)
- [ ] T022 [US4] `$PY/pytest apps/python-wiki-demo/tests/test_about.py -q` → green; `diff <($PY/wikidemo about | sed -n '/^Text from/,/^Corpus identity/p') target/xt-wiki/ATTRIBUTION.txt` → empty; paste `about`'s output into the report

---

## Phase 6: User Story 5 — The laptop agrees with the phone, on record (Priority: P2)

- [ ] T023 [US5] `apps/python-wiki-demo/wikidemo/measure.py` (D13, data-model "Measure"): `load_truth(expected_path) -> dict[qid, dict[depth, list[TruthHit]]]`; `TruthHit` (`external_id, score_bits, bm25_score_bits, dense_score_bits, rerank_score_bits, rerank_rank, rerank_combined_bits`); `truth_from_hits(hits)` (the same shape minted from live hits — for `--against`, PR B); `compare(truth, responses, depths) -> Comparison` implementing the device rule exactly (`queriesCompared`, `lexicalBitIdentical`, `fusedOrderIdentical`, `denseMaxAbsDiff`, `rerankMaxAbsDiff`, `allBitsIdentical`, `hitsCompared`, `incomplete[]`, `toleranceAbs = 1e-3`, `verdict`); `run_measure(opened, queries, depths=(0,5,10,20), k=10)` → one labelled warm-up (q01 at depth 0, not counted) then every (query, depth) with wall time, engine ms, hit count, peak bytes; `summarise(runs)` → `perDepthMedianMs`, `perDepthMaxMs`, `latency{…}` per the contract; `record(...) -> dict` with every key of `contracts/records.md` (`machine`, `os`, `python`, `xtrieverVersion`, `build`, `index` incl. `bytes` = sum of files under `index/`, `corpusIdentity`/`partial` from the sidecar or null, `openMs`, `embedderLoadMs`, `rerankerLoadMs`, `warmupMs`, `queries`, `parity`, `footprint{peakBytes, peakMethod:"ru_maxrss", ceilingBytes:600000000, underCeiling}`, `notes`, `recordedAt`); `default_record_path(paths, slice=False)`; `cli.py` gains `measure` (`--expected`, `--against` (PR B; parse now, refuse with "not yet" until Phase 7), `--queries`, `--out`; needs: artefact, embedder, reranker, expected, queries; refuses a `partial` sidecar without `--against`); prints the per-depth table, the footprint and `parity: PASS|FAIL (<counts>)`; writes the record; returns 1 on FAIL
- [ ] T024 [US5] `$PY/pytest apps/python-wiki-demo/tests/test_measure.py -q` → green (incl. the fixture-goldens PASS); then quickstart Step 4: `$PY/wikidemo measure` alone on the machine → `parity: PASS`, the record under `specs/019-python-wiki-demo/runs/`; check `latency.medianFusedMs ≤ 1000`, `medianRerankedMs ≤ 3000` (SC-001), `parity.allBitsIdentical == hitsCompared` expected on this machine (**⛔** a FAIL is stop-and-report; a bits difference with the order intact is recorded as found); paste the medians beside 009's (339 / 2,288 / 2,631) and 018's (341.5 / 1,369 / 1,704.5) into the report
- [ ] T025 [P] [US5] `apps/python-wiki-demo/README.md` (spec FR-020, PR A part): what it is (the second demo; no retrieval logic); inputs and producers (the D11 table); Step 0's install; `search` / `about` / `measure` with the flags, defaults, the 018 depth trade-off (0.4881 vs 0.4913 mean nDCG@10; 1.41 vs 2.31 s on the phone), the output layout; the record's numbers; "build" section left as a pointer to PR B; a "threads" note (`RAYON_NUM_THREADS` before the process starts)
- [ ] T026 PR A gate (quickstart Step 6): `cargo fmt --all --check && cargo clippy --workspace --all-targets && cargo nextest run --workspace && cargo deny check` (unchanged, run once); `git diff --stat main -- crates/ swift/ python/src specs/*/baselines` empty; `$PY/pytest apps/python-wiki-demo/tests -q` all green (counts into the report); `python/.venv/bin/pytest python/tests -q` untouched and green; `grep -rn "$(scutil --get ComputerName)" specs/019-python-wiki-demo apps/python-wiki-demo` → nothing; write `specs/019-python-wiki-demo/report.md` (PR A section: verdicts, the record beside 009/018, SC-001/002/003/004/006/007) and `specs/019-python-wiki-demo/pr-description.md` (PR A: what it is, the record's medians, the test counts, "no eval deltas — nothing ranking-affecting changed", the attribution line). **⛔ Commit A2**; the owner pushes and opens PR A; PR B waits for the merge

---

## Phase 7: User Story 3 — The person builds the index from scratch (Priority: P1; PR B)

**Goal**: `wikidemo build --limit N --out DIR` reproduces the Rust build's slice; `measure --against` proves it.

**Independent Test**: quickstart Step 5.

- [ ] T027 [P] [US3] Write `apps/python-wiki-demo/tests/test_chunking.py` (D5): `test_set_a_byte_identical` — for each of the 48 `chunk_a.json` cases, `chunk(body, budget, word_cost)` equals `passages` on `text`, `byte_range`, `cost` (`word_cost` = `len(words(s, 0, len(s)))`); `test_set_b_byte_identical` — for each of the 9 `chunk_b.json` articles, `chunk(body, budget, lambda u: unit_costs[u])` equals `passages`; `test_title_that_fills_the_window_is_an_error` (`documents_for` with a cost stub returning 256 for the title → `BuildError` naming the article id); `test_documents_for_shapes` — ids `"<id>#<ordinal>"`, `fields["text"] == f"{title}\n\n{passage}"`, `fields["title"]`, `ChunkInfo(parent, ordinal, byte_start, byte_end)` (stub cost = words, a two-paragraph body at a small budget → two documents)
- [ ] T028 [P] [US3] Write `apps/python-wiki-demo/tests/test_rules.py` (D6): the three manifest rules — `" (disambiguation)"` suffix; `"may refer to"` at char 10 → excluded, at char 300 → not (starts *at* the window end), at char 299 → excluded; a multi-byte prefix (`"é"×299 + "may mean"`) → the window is characters, not bytes; first match wins → index 0 when both a suffix and a lead match; `rule_name(rule)` → `title_suffix: (disambiguation)`, `lead_contains:may refer to:300`
- [ ] T029 [P] [US3] Write `apps/python-wiki-demo/tests/test_build.py` (`models`): a tmp snapshot of three JSONL articles (one plain, one `" (disambiguation)"`, one with two paragraphs) with a tmp manifest whose `jsonl.bytes`/`sha256` match the file and the three rules; `test_build_produces_the_artefact` — `run_cli(["build","--limit","3","--out",tmp/"out","--snapshot",…,"--manifest",…])` → code 0; `out/index/xtriever-pipeline.json`, `out/index/corpus.json` (schema 1, `partial == 3`, counts `articles 3, selected 2, excluded {"title_suffix: (disambiguation)": 1, …: 0}`, `passages ≥ 2`, `url_mismatches 0`), `out/ATTRIBUTION.txt` (four lines, the identity), `out/wiki-build.json` (the contract's keys); `test_identity_matches_the_record_helper`; `test_built_index_searches` — `search --artefact out "<a word from article 1>"` → code 0, the article's title and URL in stdout; `test_refuses_existing_out` (second run → code 1, `already exists`); `test_snapshot_hash_mismatch_refuses` (manifest sha edited → code 1, both hashes and `scripts/fetch-wiki.sh` on stderr); `test_limit_zero_is_a_usage_error` (code 2); `test_url_mismatch_is_an_error` (an article whose `url` differs from the derived one → code 1 naming the article)
- [ ] T030 [US3] `$PY/pytest apps/python-wiki-demo/tests/test_chunking.py tests/test_rules.py tests/test_build.py -q` → import errors; note "Red checkpoint B" in the report. **⛔ Commit B1**
- [ ] T031 [P] [US3] `apps/python-wiki-demo/wikidemo/rules.py` (D6): `@dataclass Rule(kind, value, within_chars=None)`; `parse_rules(manifest["exclusions"])`; `excluded_by(rules, title, text) -> int | None` (`lead_contains`: `window_end = len(text) if len(text) <= within_chars else text.index-of-char(within_chars)` on *character* offsets — compute `at = text.find(value)`; excluded iff `at != -1 and at < within_chars` (Python `str` offsets are characters, so the char window is the direct comparison); `rule_name(rule)`
- [ ] T032 [P] [US3] `apps/python-wiki-demo/wikidemo/chunking.py` (D5): the contract implementation copied from `reference/gen_008_fixtures.py` (`WHITESPACE`, `BLANK_LINE_INNER`, `TERMINATORS`, `is_ws`, `trim`, `collapse`, `paragraphs`, `sentences`, `words`, `byte_len`, `fragments`, `chunk`) with a module docstring naming the source and the fixtures that pin it; `WINDOW = 256`; `class Pricer` wrapping `tokenizers.Tokenizer.from_file(embedder_dir / "tokenizer.json")` with `no_truncation()`, `no_padding()`, `token_count(s)` = `len(encode(s, add_special_tokens=True).ids)`, memoised `cost(unit) = token_count(unit) - 2`; `class BuildError(Exception)`; `passage_text(title, body)`; `documents_for(article, pricer) -> list[xtriever.Document]` (budget `256 − token_count(title)`, `BuildError` when the title alone reaches 256; `Document(external_id=f"{id}#{i}", fields={"title": FieldValue.TEXT(title), "text": FieldValue.TEXT(passage_text)}, chunk=ChunkInfo(parent=id, ordinal=i, byte_start, byte_end))`)
- [ ] T033 [US3] `apps/python-wiki-demo/wikidemo/build.py` (D7–D10, data-model "Build"): `verify_snapshot(path, manifest)` (bytes + sha256 in 1 MiB chunks; `BuildError` with both hashes and the fetch command); `wiki_config()` → `IndexConfig(fields=[FieldDef("title", FieldKind.TEXT(analyzer="standard_en"), indexed=True, stored=False, boost=2.0), FieldDef("text", …, boost=1.0)], dense_fields=["text"])`; `run_build(paths, out, limit, ...)`: refuse existing `out`; remove stale `out.partial`; `IndexHandle.create(str(out.partial/"index"), config, embedder, reranker, MMAP)`; read the JSONL line by line up to `limit`, count `articles`, apply `excluded_by` (count per `rule_name`), check `wikipedia_url(title) == article["url"]` (`BuildError` otherwise, counting `url_mismatches` first), `documents_for`, buffer 4,096 documents per `handle.add(batch)` with a stderr progress line per batch (`batch i: articles r (excluded e), passages p, t s`), `commit()`, `merge()`; phases `fetch_verify, read_exclude, chunk, embed_ingest, commit, merge, total` in ms; sidecar `corpus.json` inside `index/` (D9 fields; `partial` = limit), `ATTRIBUTION.txt`, `wiki-build.json` (contracts/records.md) with `artefact_bytes` per file; rename `out.partial → out`; print the counts, phases, identity, `wrote <out>`; without `--limit`: print the full-build cost line and `time.sleep(5)` first
- [ ] T034 [US3] `cli.py`: `build` subcommand (`--out` required, `--limit` int ≥ 1 via a `type=` function raising `ArgumentTypeError` on 0, `--snapshot`, `--manifest`; needs: embedder, reranker, snapshot, manifest); `measure --against DIR` enabled: open the second artefact, mint `truth_from_hits` from its responses per (query, depth) live, compare, add the `against{…}` block (identity/counts equality from the two `corpus.json`s), `corpus: "wikipedia-slice"`, the `slice-` record name; `BuildError` → stderr + exit 1
- [ ] T035 [US3] `$PY/pytest apps/python-wiki-demo/tests -q` → all green (chunker 57 cases byte-identical — **⛔** a mismatch is stop-and-report); then quickstart Step 5: `cargo run --release -p xtriever-cli -- wiki build --limit 2000 --out target/xt-wiki-slice-rs`; `time $PY/wikidemo build --limit 2000 --out target/xt-wiki-slice-py` (note the wall time and passages; SC-005 < 30 min); the two `jq` diffs → empty (identity, snapshot, rules, chunker, counts); `$PY/wikidemo measure --artefact target/xt-wiki-slice-py --against target/xt-wiki-slice-rs` → `PASS`, `allBitsIdentical == hitsCompared` expected (D8) — record whatever is found; `about --artefact target/xt-wiki-slice-py` shows `partial: first 2000 articles`; the three negative runs (no `--limit` → the cost line, Ctrl-C; `--limit 0` → exit 2; existing `--out` → exit 1). Paste the build's counts, phases and the slice record's parity block into the report
- [ ] T036 [P] [US3] Documents (FR-020): `apps/python-wiki-demo/README.md` build section (the recipe in prose with the module names — verify, exclude, chunk, add, commit, merge, sidecars; `--limit`; the 11 h full build from 008; the slice check command and its record; the 013 note: one joined `contents` field measured +5.9 SciFact / +1.1 NFCorpus over `title`×2.0 + `text`, with the one-line `IndexConfig` change for a new corpus, and why the demo keeps 008's schema); root `README.md`: a line under the demos naming `apps/python-wiki-demo` as the second demo; `apps/ios-wiki-demo/README.md`: one pointer line; `specs/009-ios-wiki-demo/spec.md` (Assumptions) and `report.md`: a pointer to the Python demo and its host record

---

## Phase 8: Polish

- [ ] T037 PR B gate (quickstart Step 6 again): the Rust four commands (unchanged); `git diff --stat main -- crates/ swift/ python/src specs/*/baselines` empty; `$PY/pytest apps/python-wiki-demo/tests -q` all green (counts); no hostname in `specs/019-python-wiki-demo/` or `apps/python-wiki-demo/`; the slice artefacts under `target/` untracked; `git status` shows only intended files
- [ ] T038 Finish `specs/019-python-wiki-demo/report.md` (PR B section: the slice build's numbers, the parity block, SC-005; "Deliberately not done": no CI job (D18), no cache use, no `contents` schema, no persisted settings, no `--json`) and `specs/019-python-wiki-demo/pr-description.md` (PR B: the recipe in five lines, the slice record's verdict and bits count, the build time, the test counts, the attribution line). **⛔ Commit B2**; the owner pushes and opens PR B

---

## Dependencies & Execution Order

T001 → T002 → T003 → T004 → (T005 ‖ T006 ‖ T007 ‖ T008 ‖ T009 ‖ T010 ‖ T011) → T012 (A1) →
(T013 ‖ T014 ‖ T015) → T016 → T017 → T018 → T019 → T020 → T021 → T022 → T023 → T024 →
T025 → T026 (A2, PR A) → [merge] → (T027 ‖ T028 ‖ T029) → T030 (B1) → (T031 ‖ T032) →
T033 → T034 → T035 → T036 → T037 → T038 (B2, PR B).

### User story completion order

US1 → US2 → US4 → US5 (PR A) → US3 (PR B).

### Parallel opportunities

The seven PR A test files (T005–T011); the three leaf modules (T013–T015); T025 while
T024's measurement runs; the three PR B test files (T027–T029); T031 ‖ T032; T036 while
T035's slice build runs (minutes).

## Implementation Strategy

**MVP** = Phases 1–3 (`search` over the shipped index, the goldens green). **Rule 6
stop-points**: T012/T030 (red), T019 (a goldens mismatch), T024 (host parity FAIL), T035
(a chunker fixture mismatch or slice parity FAIL), T026/T037 (any gate failure). Never
the tolerance, never the fixtures, never the engine's values — rendering and the demo's own
logic only.
